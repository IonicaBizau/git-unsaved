"use strict";

/**
 * gitUnsaved
 * List the git repositories from a folder which have unsaved work.
 *
 * @name gitUnsaved
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function gitUnsaved (a, b) {
    return a + b;
};